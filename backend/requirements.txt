# GestionCardex – dépendances Python (minimales, Azure-friendly)
# ============================================================
# Régénérer ce fichier via : bash scripts/sync-requirements.sh
# (uniquement après avoir ajouté/retiré un paquet via `pip install/uninstall`)

# --- Web framework ---
fastapi==0.110.1
uvicorn==0.25.0
gunicorn==22.0.0
starlette==0.37.2

# --- Validation / sérialisation ---
pydantic==2.7.1
pydantic_core==2.18.2
typing_extensions==4.15.0
annotated-types==0.7.0

# --- ORM + Drivers SQL Server ---
SQLAlchemy==2.0.49
pymssql==2.3.1
pyodbc==5.1.0
greenlet==3.5.0

# --- Auth / Crypto ---
bcrypt==4.1.3
PyJWT==2.8.0
cryptography==42.0.7
python-jose==3.5.0
ecdsa==0.19.2
pyasn1==0.6.3
pyasn1_modules==0.4.2
rsa==4.9.1

# --- Utilitaires runtime ---
python-dotenv==1.0.1
requests==2.33.1
urllib3==2.6.3
certifi==2026.4.22
charset-normalizer==3.4.7
idna==3.13
six==1.17.0
tzdata==2026.2

# --- Génération PDF (Rapports) ---
reportlab==4.2.0
pillow==12.2.0

# --- Outils internes (anyio est dep de starlette) ---
anyio==4.13.0
sniffio==1.3.1
h11==0.16.0
click==8.3.3

# --- Tests (uniquement utile en CI, peut être dans requirements-dev.txt) ---
pytest==9.0.3
iniconfig==2.3.0
pluggy==1.6.0
packaging==26.2
