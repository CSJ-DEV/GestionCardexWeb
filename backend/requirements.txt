# GestionCardex — dépendances backend (minimales, sans paquets inutiles)
# Ce fichier contient uniquement ce qui est RÉELLEMENT importé par l'app.

# Framework HTTP
fastapi==0.110.1
uvicorn==0.25.0
starlette==0.37.2
python-multipart==0.0.27

# Configuration
python-dotenv==1.0.1

# ORM + driver SQL Server (pymssql)
SQLAlchemy==2.0.49
pymssql==2.3.1

# Validation / Modèles
pydantic[email]==2.7.1

# Sécurité
PyJWT==2.8.0
bcrypt==4.1.3
cryptography==42.0.7

# Génération PDF
reportlab==4.2.0

# Driver MongoDB (uniquement pour la fonction "Tester connexion MongoDB" — optionnel)
pymongo==4.6.3
